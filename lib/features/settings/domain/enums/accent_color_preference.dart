enum AccentColorPreference {
  system,
  defaultColor,
  playingNow,
}

enum AudioQuality {
  auto,
  high,
  low,
}

enum PlayerBackgroundStyle {
  default_,
  gradient,
  blur,
}

enum PlayerButtonsStyle {
  default_,
  primary,
  tertiary,
}

enum SliderStyle {
  default_,
  squiggly,
  slim,
}

enum GridItemSize {
  small,
  big,
}
